{
    "version": "20.2.3",
    "homepage": "https://github.com/pypa/pip",
    "description": "Package installer for Python.",
    "license": "MIT",
    "depends": "python-bucket-utils",
    "url": "https://github.com/pypa/pip/archive/20.2.3.zip",
    "hash": "72ef478414aa94af61a0c225c709fd038a13edc8364d779a25dbe5e775298f3d",
    "extract_dir": "pip-20.2.3",
    "installer": {
        "script": [
            "if ($global) { install-python-package --global --quiet --dir $dir }",
            "else { install-python-package --quiet --dir $dir }",
            "if ($LASTEXITCODE) { exit 1 }"
        ]
    },
    "uninstaller": {
        "script": [
            "if ($global) { uninstall-python-package $app --global --dir $dir }",
            "else { uninstall-python-package $app --dir $dir }"
        ]
    },
    "checkver": "github",
    "autoupdate": {
        "url": "https://github.com/pypa/pip/archive/$version.zip",
        "extract_dir": "pip-$version"
    }
}
