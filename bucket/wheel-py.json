{
    "version": "0.36.1",
    "homepage": "https://github.com/pypa/wheel",
    "description": "Implementation of the Python wheel packaging standard.",
    "license": "MIT",
    "depends": "python-bucket-utils",
    "url": "https://github.com/pypa/wheel/archive/0.36.1.zip",
    "hash": "d7f150d29a320379172c67bba8c9cf5b50cc28ad10ed21e1db91193015dc70ab",
    "extract_dir": "wheel-0.36.1",
    "installer": {
        "script": [
            "if ($global) { install-python-package --global --quiet --dir $dir }",
            "else { install-python-package --quiet --dir $dir }",
            "if ($LASTEXITCODE) { exit 1 }"
        ]
    },
    "uninstaller": {
        "script": [
            "if ($global) { uninstall-python-package $app --global --dir $dir }",
            "else { uninstall-python-package $app --dir $dir }"
        ]
    },
    "checkver": "github",
    "autoupdate": {
        "url": "https://github.com/pypa/wheel/archive/$version.zip",
        "extract_dir": "wheel-$version"
    }
}
